name = "vivo-gallery-api"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# D1 数据库绑定
[d1_databases]
binding = "DB"
database_name = "vivo-gallery-db"
database_id = "YOUR_D1_DB_ID"

# 定时触发器：每30分钟执行一次爬虫
[triggers]
crons = ["*/30 * * * *"]

# 环境变量
[vars]
VIVO_USER_ID = "YOUR_VIVO_USER_ID"
